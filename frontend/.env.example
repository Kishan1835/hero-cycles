# Local dev: leave unset — Vite proxies /api to http://localhost:4000 (see vite.config.ts).
# Production (Vercel): set to your Render API URL including /api, e.g.
# VITE_API_BASE_URL=https://hero-cycles-api.onrender.com/api
